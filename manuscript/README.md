# Manuscript (tracked in Git)

This folder holds **written report sources** for your capstone or paper:

- Markdown drafts (`*.md`)
- LaTeX source (`main.tex`)

**Not** pipeline PNGs — those go in **`figures/`** at the project root.

Large PDFs are typically **gitignored**; add them locally for submission if needed.
