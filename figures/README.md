# Figures (local — not in Git)

PNG plots from the pipeline (EDA, SHAP, stage metrics) are written here when you run `python main.py` **without** `--no-plots`.

The **EDA** page in the Streamlit app scans this folder automatically.

**Path:** project root `figures/` (not to be confused with `manuscript/`, which holds report Markdown and LaTeX).
