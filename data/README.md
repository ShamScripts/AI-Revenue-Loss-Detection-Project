# Data directory (convention)

Raw and vendor-style bundles for this submission live at the repository root as:

- **`DATASET_ieee-cis-elliptic/`** — IEEE-CIS Fraud Detection (tabular) and Elliptic Bitcoin transaction data layout used by the pipeline.

Processed tables, predictions, and experiment configs are under **`processed_data/`** (not duplicated here).
